// Single source of truth for Directus major versions with known behavioural differences.
// New features are prioritized over legacy compatibility; add a version here when a user reports version-specific behaviour.
export type DirectusMajorVersion = 'latest' | 'v11'
